---
- name: "Web Browsers | Import OS specific vars"
  ansible.builtin.include_vars: "{{ ansible_os_family | lower }}.yml"

- name: "Web Browsers | Install from package manager"
  ansible.builtin.package:
    name: "{{ web_browsers_packages }}"
    state: present
