---
- name: "Remove downloaded archives"
  ansible.builtin.file:
    path: "/tmp/{{ bat_archive_fullname }}"
    state: absent

- name: "Install assets"
  ansible.builtin.include_tasks: assets.yml

- name: "Rebuild cache"
  ansible.builtin.command: "bat cache --build"
  register: bat_out
  changed_when: bat_out.rc != 0
