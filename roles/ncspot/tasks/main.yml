---
- name: "Include OS specific variables"
  ansible.builtin.include_vars: "{{ item }}"
  with_first_found:
    - files:
        - "{{ ansible_facts.os_family | lower }}.yml"
      skip: true
  tags:
    - ncspot

- name: "Install Rust package with locked dependencies"
  community.general.cargo:
    name: ncspot
    state: latest
    locked: true
  tags: ncspot
  when: ncspot_packages is not defined

- name: "Install from package manager"
  become: true
  ansible.builtin.package:
    name: "{{ ncspot_packages }}"
  when: ncspot_packages is defined
  tags: ncspot

- name: "Copy configuration"
  tags: ncspot
  block:
    - name: "Ensure configuration directory exists"
      ansible.builtin.file:
        path: "{{ ncspot_config_dir }}"
        state: directory
        mode: "0755"

    - name: "Copy configuration file"
      ansible.builtin.copy:
        src: config.toml
        dest: "{{ ncspot_config_dir }}/config.toml"
        mode: "0644"

- name: "Copy librespot credentials"
  tags:
    - ncspot
    - librespot
  block:
    - name: "Ensure librespot cache directory exists"
      ansible.builtin.file:
        path: "{{ ncspot_librespot_cache_dir }}"
        state: directory
        mode: "0755"

    - name: "Copy configuration file"
      ansible.builtin.copy:
        src: credentials.json
        dest: "{{ ncspot_librespot_cache_dir }}/credentials.json"
        mode: "0644"

- name: "Download NcSpot desktop file"
  tags: ncspot
  become: true
  block:
    - name: "Ensure applications directory exist"
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ ansible_facts.user_dir }}/.local/share/applications"
        - "{{ ansible_facts.user_dir }}/.local/share/icons"

    - name: "Download desktop file"
      ansible.builtin.get_url:
        url: "https://raw.githubusercontent.com/hrkfdn/ncspot/refs/heads/main/misc/ncspot.desktop"
        dest: "{{ ansible_facts.user_dir }}/.local/share/applications/ncspot.desktop"
        mode: "0644"

    - name: "Download icon file"
      ansible.builtin.get_url:
        url: "https://raw.githubusercontent.com/hrkfdn/ncspot/refs/heads/main/images/logo.svg"
        dest: "{{ ansible_facts.user_dir }}/.local/share/icons/ncspot.svg"
        mode: "0644"
