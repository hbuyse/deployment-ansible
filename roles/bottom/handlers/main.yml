---
- name: "Remove bottom downloaded archives"
  ansible.builtin.file:
    path: "/tmp/{{ item }}.tar.gz"
    state: absent
  loop:
    - completion
    - manpage

- name: "Install bottom assets"
  ansible.builtin.include_tasks: assets.yml
